function Navbar() {
  return (
    <nav
      style={{
        backgroundColor: "#1e293b",
        color: "white",
        padding: "15px 25px",
        display: "flex",
        justifyContent: "space-between",
        alignItems: "center",
      }}
    >
      <h2>📊 MetricMind</h2>

      <div>
        <span>Welcome, Abhishek 👋</span>
      </div>
    </nav>
  );
}

export default Navbar;